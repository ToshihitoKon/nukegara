# i3 config file (v4)
# vim: filetype=i3config 
set $refresh_i3status killall -SIGUSR1 i3status

## Special keyboad button event
# F1-F6
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && \
            $refresh_i3status && ~/.config/i3/volnoti.sh
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && \
            $refresh_i3status && ~/.config/i3/volnoti.sh
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && \
            $refresh_i3status && ~/.config/i3/volnoti.sh
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && \
            $refresh_i3status && ~/.config/i3/volnoti.sh
bindsym XF86MonBrightnessDown exec --no-startup-id light -U 5 && $refresh_i3status
bindsym XF86MonBrightnessUp exec --no-startup-id light -A 5 && $refresh_i3status

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# start a terminal
bindsym $mod+Shift+Return exec alacritty

# Screen lock
bindsym $mod+x exec i3lock -i /home/temama/wallpapers/okayu-202306_wallpaper.png

# Screenshot
bindsym --release $mod+Shift+backslash exec import /tmp/ss.png && xclip -selection clipboard -t image/png -i /tmp/ss.png

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+p exec --no-startup-id rofi -show run

# change focus
bindsym $mod+j focus down
bindsym $mod+k focus left

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
# bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
# bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle

# focus the parent container
# bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
# bindsym $mod+1 workspace number $ws1
# bindsym $mod+2 workspace number $ws2
# bindsym $mod+3 workspace number $ws3
# bindsym $mod+4 workspace number $ws4
# bindsym $mod+5 workspace number $ws5
# bindsym $mod+6 workspace number $ws6
# bindsym $mod+7 workspace number $ws7
# bindsym $mod+8 workspace number $ws8
# bindsym $mod+9 workspace number $ws9
# bindsym $mod+0 workspace number $ws10

bindsym $mod+1 exec --no-startup-id  ~/.config/i3/workspace.sh $ws1
bindsym $mod+2 exec --no-startup-id  ~/.config/i3/workspace.sh $ws2
bindsym $mod+3 exec --no-startup-id  ~/.config/i3/workspace.sh $ws3
bindsym $mod+4 exec --no-startup-id  ~/.config/i3/workspace.sh $ws4
bindsym $mod+5 exec --no-startup-id  ~/.config/i3/workspace.sh $ws5
bindsym $mod+6 exec --no-startup-id  ~/.config/i3/workspace.sh $ws6
bindsym $mod+7 exec --no-startup-id  ~/.config/i3/workspace.sh $ws7
bindsym $mod+8 exec --no-startup-id  ~/.config/i3/workspace.sh $ws8
bindsym $mod+9 exec --no-startup-id  ~/.config/i3/workspace.sh $ws9
bindsym $mod+0 exec --no-startup-id  ~/.config/i3/workspace.sh $ws0

# move focused container to workspace
bindsym $mod+Shift+$left move window to workspace next
bindsym $mod+Shift+$right move window to workspace prev
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

bindsym $mod+r mode "resize"

# move focus workspace
bindsym $mod+$left workspace prev
bindsym $mod+$right workspace next


# resize
bindsym $mod+Ctrl+$right resize shrink width 5 px or 5 ppt
bindsym $mod+Ctrl+$left resize grow width 5 px or 5 ppt

# Toggle the current focus between tiling and floating mode
bindsym $mod+Shift+space floating toggle
